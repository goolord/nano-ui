nano-ui frame-allocation plan: final report
date: 2026-09-12 (Linux x86_64, GHC 9.14.1, -O2 -mavx2, RTS -N1 -A64m -T -I0)
NOTE: the X display died partway through the session; the last measurements
use SDL_VIDEODRIVER=dummy SDL_RENDER_DRIVER=software, which was verified to
give identical ui/runFrame allocations to the real driver (194/394 KB on the
same tree). The demo geometry prints differ by 4 verts under the dummy driver
(font fallback), but a real-driver run of the same tree before the display
died was byte-identical to baseline.

FINAL ACCEPTANCE
----------------
Allocation <=30% of baseline for the fixed workload:  MET
  ui/runFrame medium  455 -> 134 KB  (29.5% of baseline, -70.5%)
  ui/runFrame large   945 -> 272 KB  (28.8% of baseline, -71.2%)
  ui/runFrame small    17 ->  11 KB  (64.7% of baseline)
No interaction/rendering regressions:  MET
  cabal test all PASS; demo draw geometry byte-identical with and without
  layout reuse at every step; new cached-vs-uncached tests for layout,
  scroll and deep nesting.
Improved CPU frame time / no worse tail:  MET (noise-level; solve skipped)
  medium ~188 -> 174 us, large ~408 -> 374 us.
Bounded caches and retained memory:  MET
  LayoutCache arrays preallocated + geometrically grown; span/widget-text
  caches cleared on font/theme/DPI change; per-depth axis snapshots grow
  on demand instead of clamping.

PHASES / CHANGES
----------------
Phase 0  baseline                      profiles/results/16-plan-baseline
Phase 1  ptr-eq store diff + typed writes                        79dfc5c
Phase 2  widget-text placement cache + batched glyph reservations 864998a 3126c7b
Phase 3  fused rounded geometry, strict loops, hot-path INLINE    dca6eea 77503c1
         + INLINE pass (resolveInteraction etc.)                  b9bf0f4..c3ea7df
Phase 4  dense primitive storage       not implemented (target met without it)
Phase 5A whole-layout reuse            LayoutCache + exact descriptor compare,
         restore solved rects; metric-generation invalidation     986358d
Phase 5B scroll reuse + snapshot growth
         scroll nodes compared minus solver columns 12/13, full style
         restore; ensureAxisSnapshot grows levels safely           fffba49 485af4b
Tests    cached-vs-uncached layout/scroll, deep-nesting             bbc8cbb

DEMO (SDL profile, baseline -> final alloc/frame)
  Full DemoUi runFrame  509.7 -> 264.4 KB
  Tab Controls          157.9 ->  72.5 KB
  Tab List (Tree+Items) 135.9 ->  66.5 KB
  Tab Table (14x5)      421.0 -> 230.6 KB
  100x Button           534.9 -> 214.2 KB
  100x Box              206.6 ->  52.1 KB
  100x Select           682.0 -> 276.0 KB
  50x Nested Rows       390.0 ->  90.9 KB
  20x ColorPicker      1626.5 -> 709.0 KB
  Table 50 rows         631.9 -> 358.9 KB
  Table 200 rows        826.1 -> 553.1 KB

REMAINING (not required by the acceptance)
  - Phase 4 dense primitive widget storage (superseded here by the INLINE
    pass + layout reuse).
  - Full Phase 5B dirty split (measureDirty/arrangeDirty/paintDirty); only
    whole-layout reuse incl. scroll is implemented.
  - Floating windows/modals/popups still take the full solve each frame.
  - Deferred: shader/SDF, nine-slice chrome, mirror-pass redesign,
    retained-pixel scrolling.
EOF
